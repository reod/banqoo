import { PlncurrencyPipe } from './plncurrency.pipe';

describe('PlncurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new PlncurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
